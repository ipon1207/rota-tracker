# DB環境の構築

## テーブルのスキーマ

- [車輪の再発明トラッカー - 移植設計](../../車輪の再発明トラッカー_移植設計.md)

### ER図

```mermaid
erDiagram
  category ||--o{ project : "分類する"
  project  ||--o| guide : "学習ガイドを持つ"
  guide    ||--o{ guide_step : "手順を持つ"
  project  ||--o{ project_keyword : "キーワードを持つ"
  glossary_term ||--o{ project_keyword : "参照される"
  roadmap_route ||--|{ roadmap_stop : "停車駅を持つ"
  project  ||--o{ roadmap_stop : "駅として登場する"
  project  ||--o| entry : "進捗記録を持つ"
  entry    ||--o{ entry_step : "ステップの達成状況を持つ"

  category {
    NVARCHAR(20) id PK "NOT NULL"
    NVARCHAR(50) name "NOT NULL"
    INT sort_order "NOT NULL"
  }

  project {
    NVARCHAR(30) id PK "NOT NULL"
    NVARCHAR(20) category_id FK "NOT NULL"
    NVARCHAR(120) title "NOT NULL"
    TINYINT difficulty "NULL"
    INT sort_order "NOT NULL"
  }

  guide {
    NVARCHAR(30) project_id PK,FK "NOT NULL"
    NVARCHAR(400) goal "NOT NULL"
    NVARCHAR(800) learn "NOT NULL"
  }

  guide_step {
    NVARCHAR(30) project_id PK,FK "NOT NULL"
    INT step_no PK "NOT NULL"
    NVARCHAR(400) body "NOT NULL"
  }

  glossary_term {
    INT id PK "IDENTITY"
    NVARCHAR(120) term UK "NOT NULL"
    NVARCHAR(1000) description "NOT NULL"
    NVARCHAR(30) demo_kind "NULL"
  }

  project_keyword {
    NVARCHAR(30) project_id PK,FK "NOT NULL"
    INT term_id PK,FK "NOT NULL"
    INT sort_order "NOT NULL"
  }

  roadmap_route {
    NVARCHAR(20) id PK "NOT NULL"
    NVARCHAR(60) name "NOT NULL"
    NVARCHAR(200) description "NOT NULL"
    INT sort_order "NOT NULL"
  }

  roadmap_stop {
    NVARCHAR(20) route_id PK,FK "NOT NULL"
    INT position PK "NOT NULL"
    NVARCHAR(30) project_id FK "NOT NULL"
  }

  entry {
    NVARCHAR(30) project_id PK,FK "NOT NULL"
    NVARCHAR(10) status "NOT NULL / CHECK (todo,doing,done)"
    NVARCHAR(60) lang "NULL"
    NVARCHAR(MAX) memo "NULL"
    NVARCHAR(500) repo_url "NULL"
    DATE start_date "NULL"
    DATE done_date "NULL"
    DATETIME2 updated_at "NOT NULL"
  }

  entry_step {
    NVARCHAR(30) project_id PK,FK "NOT NULL"
    INT step_no PK "NOT NULL"
    BIT is_checked "NOT NULL"
  }
```

## SQL Serverで NULL 許容カラムを定義する

```SQL
CREATE TABLE Users (
    Id INT IDENTITY(1,1) PRIMARY KEY,
    Name NVARCHAR(50) NOT NULL,    -- NULLを許可しない（必須）
    Email NVARCHAR(100) NULL,      -- NULLを許可する
    Nickname NVARCHAR(50)          -- 何も書かない場合はNULLを許可
);
```

## SQL Serverで FOREIGN KEY を定義する

- [SQL Serverで学ぶFOREIGN KEY制約：外部キーでデータの整合性を確保する方法](https://beagle-dog.com/sql-server-foreign-key-guide/)

```SQL
CREATE TABLE Orders (
    OrderID INT PRIMARY KEY,
    OrderDate DATE NOT NULL,
    CustomerID INT,
    FOREIGN KEY (CustomerID) REFERENCES Customers(CustomerID) ON DELETE CASCADE
);
```

## SQL Serverで UNIQUE 制約を設ける

- [SQL ServerのUNIQUE制約とは？特定の列の値が重複しないようにする方法と設定手順](https://beagle-dog.com/sql-server-unique-constraint/)

```SQL
CREATE TABLE Users (
    user_id INT PRIMARY KEY,
    email NVARCHAR(255) UNIQUE,
    username NVARCHAR(50)
);
```

## SQL Serverで CHECK 制約を設ける

- [SQL ServerのCHECK制約を完全解説：データの整合性を保つ方法](https://beagle-dog.com/sql-server-check-constraints/)

```SQL
CREATE TABLE Customers (
    CustomerID INT PRIMARY KEY,
    Gender CHAR(1),
    CONSTRAINT CK_Gender CHECK (Gender IN ('M', 'F'))
);
```
