# 車輪の再発明トラッカー ─ 移植設計

単一ファイルの React アプリ（クライアント完結・`window.storage` 保存）を、
**SQL Server + ASP.NET Core + React** の3層構成に作り変えるための設計。
目的は完成そのものより、指定スタックを一通り通すこと。

---

## 1. 何が変わるか

| | 現状 | 移植後 |
|---|---|---|
| マスタデータ（45プロジェクト・ガイド・用語集・ロードマップ） | JSX 内の const | SQL Server のテーブル → API |
| 進捗記録 | `window.storage` に JSON 1件 | `entry` / `entry_step` テーブル |
| 画面 | 1コンポーネント内の `view` state | React Router の4ルート |
| 一覧 | 手書きのアコーディオン | TanStack Table |
| スタイル | 450行の `<style>` | Tailwind + shadcn/ui |
| デモ（ライフゲーム等） | そのまま | **そのままクライアント側**（Vitest の対象にする） |

マスタは「ほぼ変わらない読み取り専用データ」、記録は「頻繁に書き換わるデータ」。
この性質の違いが、そのまま TanStack Query のキャッシュ戦略の練習材料になる。

---

## 2. スコープ

**MVP に入れる**
- カテゴリ／プロジェクト一覧、ステータス3状態（未着手・進行中・完了）の切り替え
- 記録の編集（使った言語・リポジトリURL・着手日・完了日・メモ）
- 学習ガイドの表示と、進め方ステップのチェック
- 用語集（検索・プロジェクトからの相互リンク）
- ロードマップ（ルート表示・プロジェクトへ遷移）
- 進捗集計（全体％、カテゴリ別）

**後回し／対象外**
- 認証・複数ユーザー（当面は単一ユーザー固定。やるなら最終フェーズ）
- 用語集のインタラクティブデモ（移植時は現行コードをほぼそのまま移す）
- 車輪SVG（見た目の楽しみ部分。急がない）

---

## 3. 技術スタック → 担当箇所

学習が目的なので、**各技術に必ず1つ以上「それでしかできない仕事」を割り当てる**。

| 技術 | この機能で使う／学ぶ |
|---|---|
| TypeScript | ドメイン型定義。`status` を判別可能なユニオンにして分岐漏れを型で潰す |
| Vite | 開発サーバ、`/api` の proxy 設定、環境変数の扱い |
| React Router | 4ルート。フィルタ状態を `useSearchParams` に置いてURLで共有可能にする |
| TanStack Query | マスタ（`staleTime` 長め）と記録（都度更新）のキャッシュ戦略、`queryKey` 設計、楽観的更新、`invalidateQueries` |
| TanStack Table | 45行の一覧。難易度・状態でのソート、全文検索、カテゴリでのグルーピング、列の表示切替 |
| Base UI | タブ・ダイアログ・ポップオーバーの挙動レイヤ |
| shadcn/ui | 上記の見た目レイヤ（Button / Input / Badge / Tabs / Table）。2026年7月以降 `shadcn init` の既定土台は Base UI なので、両者は競合せず素直に重なる |
| Tailwind CSS | 現行の `<style>` を全置換。CSS変数（`--accent` 等）はテーマトークンへ寄せる |
| Zod | 記録フォームの検証（URL形式、着手日 ≤ 完了日）。API 境界でのレスポンス検証にも |
| openapi-typescript | .NET が出力する `openapi.json` から型生成。**フロントで API 型を手書きしない** |
| Oxlint / Oxfmt | 設定ファイル、pre-commit、CI での実行 |
| Vitest | 純粋ロジックの単体テスト（進捗率、ステータス遷移、ライフゲーム1世代、LRU、トポロジカルソート） |
| Playwright | 主要導線のE2E（後述） |
| C# / .NET 10 / ASP.NET Core | Minimal API でのエンドポイント定義、DI、モデルバインド、例外ハンドリング |
| Dapper | multi-mapping（カテゴリ＋プロジェクト）、`QueryMultiple`、upsert のトランザクション |
| Microsoft.Data.SqlClient | 接続の生成と寿命、パラメータ化クエリ |
| AspNetCore.OpenApi | OpenAPI ドキュメント生成、ビルド時のファイル出力 |
| xUnit | サービス層の単体テスト、`WebApplicationFactory` による統合テスト |

---

## 4. データモデル（SQL Server）

### マスタ（シードで投入、アプリからは読み取りのみ）

```
category(id NVARCHAR(20) PK, name NVARCHAR(50), sort_order INT)

project(id NVARCHAR(30) PK, category_id NVARCHAR(20) FK,
        title NVARCHAR(120), difficulty TINYINT NULL, sort_order INT)

guide(project_id NVARCHAR(30) PK/FK, goal NVARCHAR(400), learn NVARCHAR(800))

guide_step(project_id NVARCHAR(30), step_no INT, body NVARCHAR(400))
        PK(project_id, step_no)

glossary_term(id INT IDENTITY PK, term NVARCHAR(120) UNIQUE,
              description NVARCHAR(1000), demo_kind NVARCHAR(30) NULL)

project_keyword(project_id NVARCHAR(30), term_id INT, sort_order INT)
        PK(project_id, term_id)

roadmap_route(id NVARCHAR(20) PK, name NVARCHAR(60),
              description NVARCHAR(200), sort_order INT)

roadmap_stop(route_id NVARCHAR(20), position INT, project_id NVARCHAR(30))
        PK(route_id, position)
```

`project_keyword` が肝。ガイドのキーワードチップ → 用語集、用語集 → 使っているプロジェクト、
という双方向リンクが1テーブルの JOIN 方向違いで表現できる（逆引きの SQL がそのまま練習になる）。

### 記録（アプリが書き換える）

```
entry(project_id NVARCHAR(30) PK/FK,
      status NVARCHAR(10) NOT NULL CHECK (status IN ('todo','doing','done')),
      lang NVARCHAR(60) NULL, memo NVARCHAR(MAX) NULL, repo_url NVARCHAR(500) NULL,
      start_date DATE NULL, done_date DATE NULL,
      updated_at DATETIME2 NOT NULL)

entry_step(project_id NVARCHAR(30), step_no INT, is_checked BIT)
        PK(project_id, step_no)
```

判断ポイント：`entry_step` は `entry.steps` を JSON 列にしても動く。
正規化しておくと「チェック1個だけ更新」が自然に書けるので、こちらを推す。

### シードデータの作り方

現行 JSX の `CATEGORIES` / `GUIDES` / `GLOSSARY` / `ROADMAP` をそのまま使える。
Node スクリプトで JSX の const を読み、`seed.sql` の INSERT 文を吐かせるのが最短。
手打ちは45プロジェクト＋用語100件超で確実に破綻する。

---

## 5. API 設計

```
GET    /api/categories                        カテゴリ＋所属プロジェクト（ネスト）
GET    /api/projects                          フラットな一覧（テーブル用・難易度込み）
GET    /api/projects/{id}                     ガイド・ステップ・キーワード込みの詳細
GET    /api/glossary                          用語＋関連プロジェクトID
GET    /api/roadmap                           ルート＋停車駅
GET    /api/progress                          状態別・カテゴリ別の集計

GET    /api/entries                           記録の全件
PUT    /api/entries/{projectId}               記録の upsert（フォーム保存）
PATCH  /api/entries/{projectId}/status        ステータス遷移のみ
PUT    /api/entries/{projectId}/steps/{stepNo} ステップのチェック
DELETE /api/entries                           全消去
```

設計意図：
- `PATCH .../status` を分けたのは、行クリック1発の操作に**楽観的更新**を効かせるため。
  `PUT` の全体保存とは更新頻度もUXも違う。この2本立てが TanStack Query の練習の中心になる。
- ステータス遷移に伴う着手日・完了日の自動セットは**サーバ側の責務**に寄せる
  （現行は `cycleStatus` 内のクライアントロジック）。xUnit のテスト対象になる。
- `GET /api/progress` は集計をクライアントでやっても出せるが、あえて `GROUP BY` で書く。

---

## 6. 画面とルーティング

```
/                     ダッシュボード（進捗％、状態別件数、次にやる1件の提案）
/projects             一覧（TanStack Table）  ?status=&category=&q=
/projects/:projectId  詳細（「学習ガイド」「記録」タブ）
/roadmap              ルート表示
/glossary             用語集              ?q=&demo=1#term-<id>
```

現行アプリの `view` / `filter` / `openId` / `glossaryQuery` といった state は、
**ほぼ全部 URL に移す**。ロードマップの駅 → プロジェクト詳細、キーワードチップ → 用語集、
という相互リンクが `<Link>` 1行になり、リロードしても状態が戻る。

---

## 7. ディレクトリ構成

```
wheel-tracker/
├─ api/
│  ├─ WheelTracker.Api/         Minimal API・DI・OpenAPI
│  │   ├─ Endpoints/            Projects / Entries / Glossary / Roadmap
│  │   ├─ Features/             ドメインロジック（ステータス遷移など）
│  │   └─ Data/                 Dapper のリポジトリ・SQL
│  └─ WheelTracker.Tests/       xUnit
├─ db/
│  ├─ schema.sql
│  ├─ seed.sql                  ← 生成物
│  └─ tools/generate-seed.mjs   JSX → INSERT 変換
└─ web/
   ├─ src/
   │  ├─ routes/                ルート単位の画面
   │  ├─ features/              projects / entries / glossary / roadmap
   │  ├─ components/ui/         shadcn/ui
   │  ├─ lib/api/               生成型 + fetch ラッパ
   │  └─ demos/                 用語デモ（純粋ロジックは別ファイルに分離）
   └─ e2e/                      Playwright
```

デモは「純粋関数」と「表示コンポーネント」に必ず割る。
ライフゲームの1世代計算や LRU の更新を関数として切り出せば、そのまま Vitest の題材になる。

---

## 8. 実装フェーズ

各フェーズの終わりに「動くもの」が残るように区切ってある。

| # | やること | 終了条件 | 主に触る技術 |
|---|---|---|---|
| 0 | 環境構築。Vite+TS+Tailwind+shadcn、.NET ソリューション、SQL Server（LocalDB か Docker）、Oxlint/Oxfmt | 空の画面と `/api/health` が両方立ち上がる | Vite, Tailwind, shadcn, Oxlint |
| 1 | `schema.sql` + シード生成 + 読み取りAPI（categories / projects） | Swagger 相当で45件返る | SQL Server, Dapper, SqlClient, AspNetCore.OpenApi |
| 2 | `openapi.json` → 型生成 → TanStack Query で一覧表示 | 画面にDB由来の45件が並ぶ | openapi-typescript, TanStack Query |
| 3 | 一覧を TanStack Table 化＋React Router で画面分割 | ソート・検索・カテゴリ絞り込みがURLに乗る | TanStack Table, React Router |
| 4 | 記録の読み書き（PUT / PATCH）、Zod 検証、楽観的更新 | ステータス変更が即反映され、リロードしても残る | Zod, TanStack Query, ASP.NET Core, xUnit |
| 5 | ガイド・ステップチェック・用語集・ロードマップ | 相互リンクが全部つながる | Base UI, Dapper（JOIN） |
| 6 | テスト整備（Vitest / xUnit / Playwright）と CI | 主要導線が緑 | Vitest, Playwright, xUnit |
| 7 | 任意：デモ移植、車輪SVG、複数ユーザー化 | ― | ― |

フェーズ2が最初の山（ビルド → OpenAPI出力 → 型生成 の流れを自動化するところ）。
ここを `npm run gen` 一発にしておかないと、以降ずっと手間が効いてくる。

---

## 9. テスト方針

- **Vitest**：進捗率の計算、ステータス遷移表、用語→プロジェクト逆引き、ライフゲーム1世代、LRU、トポロジカルソート。UIではなく純粋関数に寄せる。
- **xUnit**：ステータス遷移時の日付自動セット（単体）、`WebApplicationFactory` で `PUT /api/entries/{id}` の契約テスト（統合）。
- **Playwright**：3本で十分。
  1. 一覧でプロジェクトを開き → ステータスを完了にする → リロードして残っている
  2. 記録フォームに不正なURLを入れる → エラー表示 → 直して保存できる
  3. ロードマップの駅をクリック → 詳細へ遷移 → キーワードから用語集へ飛ぶ

---

## 10. 先に決めておくこと

1. **単一ユーザー前提でいくか**。当面 Yes を推す。認証を入れるとフェーズ4以降が倍になる。
2. **SQL Server をどう立てるか**。LocalDB か Docker の `mssql/server`。統合テストで使うなら Docker のほうが後々楽。
3. **日付の扱い**。`DATE` 型でタイムゾーンを持たせない（現行の `toISOString().slice(0,10)` と揃う）。
4. **マスタ変更をどう扱うか**。当面はシード再実行で作り直す前提にし、マイグレーション管理は持ち込まない。
